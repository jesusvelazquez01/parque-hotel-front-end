
// Standard animation durations for consistent use across the project
export const ANIMATION_DURATIONS = {
  fast: "duration-150",
  default: "duration-300",
  slow: "duration-500",
  extraSlow: "duration-700",
};

// You can use these constants instead of arbitrary values like duration-[1000ms]
// Example: className={`transition-all ${ANIMATION_DURATIONS.slow}`}
